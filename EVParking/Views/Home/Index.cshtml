@{
    ViewData["Title"] = "Home Page";
}

<dl>
    @{
        Station s = new();
        List<Station> stations = await s.Get();
        foreach(Station station in stations)
        {
            <dt>@station.Name</dt>
            <dt>@station.Status.ToString()</dt>
        }
    }
</dl>